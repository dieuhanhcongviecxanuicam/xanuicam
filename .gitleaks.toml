[rules]

[[rules.rule]]
description = "Generic API key"
regex = "(?i)(?:api[_-]?key|token|secret|password)\s*[:=]\s*['\"]?[A-Za-z0-9\-_=+\\/]{16,}['\"]?"
tags = ["apikey"]

[[rules.allowlist]]
description = "Allow local test artifacts"
regex = "^tmp/|^/tmp/|^docs/|^e2e/"
