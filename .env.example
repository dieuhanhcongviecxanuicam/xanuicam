## IMPORTANT: This file is a template. Do NOT place real secrets here.
## Copy this file to `.env` (root) and fill values locally. Ensure `.env`
## is listed in `.gitignore` and never committed or pushed to a remote.

###### JWT / Security
# JWT signing secret: use a long, random value (recommended: 64+ bytes).
# Use a secure generator and store the real secret in CI secrets or a vault.
JWT_SECRET=

###### Database (used by docker-compose and local dev)
# DB values are referenced by docker-compose via environment substitution.
DB_USER=ubnd
DB_PASSWORD=changeme
DB_DATABASE=ubndxanuicam
DB_HOST=postgres
DB_PORT=5432

###### Backend runtime
PORT=5000
NODE_ENV=development
# Allow backend to serve static frontend when `SERVE_FRONTEND=true` (dev only)
SERVE_FRONTEND=false

###### Superadmin (DEV ONLY)
# Keep this empty in production. Use seed scripts to create admin accounts.
SUPERADMIN_SECRET=

###### Cloud / Deploy (store real values in CI secrets, not here)
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

###### Other runtime flags / paths
SKIP_DEVICE_LIMIT=1
SOFFICE_PATH=
EXPORT_TMP_DIR=

# Notes:
# - For CI and production deploys, populate these values from your CI provider
#   secret store (GitHub Secrets, GitLab CI variables, Azure Key Vault, etc.).
# - Never check a file containing values for `DB_PASSWORD`, `JWT_SECRET`,
#   `CLOUDFLARE_API_TOKEN` or other credentials into git.
