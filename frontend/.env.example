# Frontend environment variables example
# Map provider: 'leaflet' (default) or 'google'
REACT_APP_MAP_PROVIDER=leaflet
# Google Maps API key (only required if REACT_APP_MAP_PROVIDER=google)
REACT_APP_GOOGLE_MAPS_API_KEY=
