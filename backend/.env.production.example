### Production backend env example (DO NOT COMMIT REAL SECRETS)
# Copy to backend/.env on the production host and populate with secrets

# Database
DB_HOST=postgres
DB_PORT=5432
DB_USER=ubndxanuicam
DB_PASSWORD=<SET_SECURE_DB_PASSWORD_IN_ENV>
DB_DATABASE=ubndxanuicam

# JWT and audit keys
JWT_ALGORITHM=HS256
JWT_SECRET=<SET_SECURE_JWT_SECRET_IN_ENV>
AUDIT_LOG_KEY=<SET_BASE64_32_BYTE_KEY_IN_ENV>

# Runtime
NODE_ENV=production
PORT=8080
SERVE_FRONTEND=true
