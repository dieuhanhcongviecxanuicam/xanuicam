### Example backend .env (DO NOT commit real secrets)
### Copy this to `backend/.env` on the host or generate with the helper script.

# Server
NODE_ENV=development
PORT=5000
ALLOW_LOCALHOST=true

# JWT (generate and store in secret manager / env)
JWT_SECRET=<SET_SECURE_JWT_SECRET_IN_ENV>

# Database (do not store passwords in repo)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=<SET_SECURE_DB_PASSWORD_IN_ENV>
DB_DATABASE=ubndxanuicam

# Optional flags for development diagnostics
REQUEST_LOGGING=false
DEBUG_LOGS=false

# Audit encryption key (32 bytes base64 recommended)
AUDIT_LOG_KEY=<SET_BASE64_32_BYTE_KEY_IN_ENV>

# Local-only overrides should go into backend/.env.local which is ignored
