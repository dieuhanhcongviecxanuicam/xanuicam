# Example environment variables for backend (copy to `.env` before running)
# This repository's `.gitignore` intentionally ignores dot-files like `.env.example`.
# Use this file as the reference copy: `cp backend/env.example backend/.env`.

NODE_ENV=development
PORT=3000

# Model selection for Anthropic/Claude usage
CLAUDE_MODEL=claude-haiku-4.5

# Add `ANTHROPIC_API_KEY` to your deployment environment or GitHub Secrets
# ANTHROPIC_API_KEY=
