# cloudflared config example (DO NOT COMMIT real tokens)
# Copy to server: /etc/cloudflared/config.yml or ~/.cloudflared/config.yml

# Example using a tunnel
# Replace placeholders with real values on the server (never commit)

tunnel: 93f6a21b-38d6-4ff7-a562-9c0ae99d7bef
credentials-file: /etc/cloudflared/.cloudflared/93f6a21b-38d6-4ff7-a562-9c0ae99d7bef.json

ingress:
  - hostname: dev.xanuicam.vn
    service: http://localhost:3000
  - service: http_status:404

# Optional: expose metrics locally
metrics: localhost:2000

# Note: for token-based API usage, keep tokens in environment or protected files.
# Do not place `user_api_token` or `origin_certificate` in this example file.
